{
  "name": "same-model-same-task",
  "version": "0.1.0",
  "description": "Cross-language benchmark of agent frameworks (Python + TypeScript) with same model + task + tools",
  "private": true,
  "type": "module",
  "scripts": {
    "tools:test": "bun run tools/typescript/src/tools.test.ts",
    "baseline": "bun run frameworks/baseline/typescript/run.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "^2.0.47",
    "@mastra/core": "^1.32.1",
    "ai": "^6.0.175",
    "openai": "^6.37.0",
    "zod": "^4.4.3"
  }
}
